<% if (item && item.img && item.img.src) { %>
    <img class="project-thumbnail" src="<%= item.img.src %>" alt="<%= item.img.alt ?? item.title %>" />
<% } else { %>
    <img class="project-thumbnail" src="https://via.placeholder.com/150" alt="Missing Project Thumbnail" />
<% } %>